# Legacy website archive

These files are retained for reference but are intentionally stored outside
`website/public`, so Vite and Vercel do not publish them with the current site.

The active public website source lives in `website/src` and the small set of
assets and standalone pages still required at runtime lives in `website/public`.
